# 验证报告：被动章节解锁改造 + 被动 Bug 修复

- 任务: t_361bcddd（验证，程序实现来自 t_1afbbdc4）
- 验证人: profile_unity-tester
- 日期: 2026-08-03
- 项目: G:\unity\Tuanjie project\My project 2D\project
- 验证方式: 强制重编译（dotnet Rebuild）+ 自动化逻辑验证程序（G:\unity\verify_passive，50 PASS / 0 FAIL / 1 SKIP）+ 源码审查 + 场景序列化核对

---

## 一、改动对照确认（5 个文件全部命中任务描述）

| 文件 | 任务描述 | 实际代码 | 核对 |
|---|---|---|---|
| PassiveEquipManager.cs | 等级解锁删除→currentChapter(1~5) 章节解锁 | `currentChapter=1` 字段（L82）；无 unlockLevels 字段 | ✔ |
| | UnlockChapterOf 具名映射 T1→1章…T5→5章 | `UnlockChapterOf(layer)` switch（L240-248）0→1,1→2,2→3,3→4,4→5 | ✔ |
| | IsLayerUnlocked = currentChapter >= UnlockChapterOf | `IsLayerUnlocked` = valid && currentChapter >= UnlockChapterOf(layer)（L251-252） | ✔ |
| | 新增 SetChapter / AdvanceChapter / RestorePassiveSlots（跳过解锁检查） | SetChapter（L295）、AdvanceChapter（L283）、RestorePassiveSlots→EquipPassiveInternal（L531-567，无 IsLayerUnlocked 检查） | ✔ |
| PassiveUI.cs | 层级标题/锁覆盖文字 Lv→第X章解锁 | SetLayerState `[第{unlockChapter}章解锁]`（L143）、unlockLabel `第{unlockChapter}章解锁`（L184） | ✔ |
| | GetUnlockLevel→GetUnlockChapter | GetUnlockChapter（L199-202）→ passiveEquipManager.GetUnlockChapter | ✔ |
| | 槽位右键卸下(EventTrigger) | BindSlotButtons EventTrigger PointerClick + Right 按钮 → UnequipPassive（L67-83） | ✔ |
| SaveSystem.cs | SaveData 增 currentChapter=1 | SaveData.currentChapter=1（L574）；保存 L78；恢复 L380 先 SetChapter 再 RestorePassiveSlots（L381→L389） | ✔ |
| PlayerController.cs | OnCombatAction 防重(combatTimer>0 只刷新计时器) | `if (combatTimer > 0f) { combatTimer = CombatExitDelay; return; }`（L339-343） | ✔ |
| Events.cs | 新增 ChapterChangedEvent | `readonly struct ChapterChangedEvent`（L384-393），SetChapter/AdvanceChapter 触发 | ✔ |

---

## 二、验证清单结果

| # | 验证项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 代码编译无错误 | **PASS** | `dotnet build -t:Rebuild Assembly-CSharp.csproj` = 0 错误；`Assembly-CSharp-Editor.csproj` = 0 错误；Unity 编辑器编译产物 Assembly-CSharp.dll（Library/ScriptAssemblies, 10:14）无 CS 错误日志 |
| 2 | 进游戏打开被动面板：T1 解锁，T2~T5 显示"第X章解锁"且槽位锁定 | **PASS**（静态审查） | currentChapter 默认 1（字段初始化器）；Refresh→SetSlotState `interactable = unlocked && !InCombat`（L156）；unlockLabel 仅 !unlocked 显示（L183-185）；lockOverlays 仅 !interactable 激活（L178）。场景 SampleScene.scene 全部 15 槽/5 层引用齐全（slotButtons/slotIcons/slotLineNames/slotEffects/lockOverlays/unlockLabels 各 15，layerTitles/layerLockIcons 各 5，lineSelectDialog/lineDialogOptions 5） |
| 3 | 控制台 SetChapter(3) → T1/T2/T3 解锁，T4/T5 仍锁 | **PASS** | 验证程序 [3]：SetChapter(3) 后 CurrentChapter==3，T1/T2/T3 解锁，T4/T5 锁定；SetChapter(99)→5、SetChapter(-5)→1 钳制正确 |
| 4 | 装备 T2 → 存档 → 读档 → T2 仍在 | **PASS** | 验证程序 [11]：第2章装备 T2(line0)→快照→新实例 SetChapter(2)+RestorePassiveSlots→T2 槽0=line0 仍在；且第1章读档跨章节数据仍恢复（跳过解锁检查）。[8] 亦覆盖 T4/T5 锁定层被动恢复 |
| 5 | 右键已装备槽位 → 被动卸下，槽位变空 | **PASS**（静态审查） | PassiveUI.cs L67-83：EventTrigger PointerClick 且 ped.button==Right → GetEquippedLineId≥0 → UnequipPassive → Slots[..]=-1 → PassiveSlotsChangedEvent → Refresh 槽位清空。Button.onClick 仅响应左键（Unity 内置），右键不会误开选线弹窗 |
| 6 | 连续攻击 3 次以上 → 脱离战斗等 3 秒 → 面板槽位可点击（防重生效） | **PASS** | 验证程序 [9]：3 次 OnCombatAction 后 combatRefCount==1（只 +1 一次）、combatTimer 刷新为 3s；模拟 3s 递减后 inCombat==false、combatRefCount==0。配合 PlayerCombat.TryAttack 的 playerCombatFlag 守卫（L524-528），进出配对平衡，不再永久卡战斗态 |
| 7 | 旧存档（无 currentChapter）读档 → 默认章节 1，T1 解锁，不崩不丢数据 | **PASS** | 双保险：SaveData 字段初始化器 currentChapter=1（L574）+ SaveSystem 兜底 `data.currentChapter > 0 ? : 1`（L380）。验证程序 [8] 模拟 currentChapter=0 路径：章节默认 1、T1 槽位恢复、不丢数据。JsonUtility 本机纯 .NET 执行受 ECall 限制（SKIP），已由源码双路径审查覆盖 |
| 8 | 战斗时打开被动面板 → 槽位置灰不可点 | **PASS**（静态审查） | SetSlotState `interactable = unlocked && !passiveEquipManager.InCombat`（L156）→ 战斗中全部置灰+overlay；OpenLineDialog（L90-92）、EquipPassive（L152-156）、UnequipPassive（L189-191）均有 InCombat 守卫，三层防御 |

**汇总：8/8 通过（其中 3 项为静态审查 + 场景核对，5 项为自动化逻辑验证 + 编译验证）。未发现阻断性缺陷。**

---

## 三、Bug 列表

### **Bug #1**（次要 / 仅日志文案）
- 位置: `Assets/Scripts/Skills/PassiveEquipManager.cs:520`
- 环境: 任意（运行时触发 EquipPassive 失败警告时）
- 现象: `LayerLabel` 用 `"T" + (char)('I' + layer)` 生成层级名，layer≥1 时输出 TJ/TK/TL/TM，正确应为 TII/TIII/TIV/TV
- 复现步骤: 第1章打开面板 → 控制台执行 `passiveEquipManager.EquipPassive(1, 0, 0)` → Console 警告显示 `层级 TJ 未解锁…`
- 预期: `层级 TII 未解锁…`；实际: `层级 TJ 未解锁…`
- 影响: 仅 Debug.LogWarning 文案错误，不影响解锁/装备逻辑（UI 使用 ToRoman 正确）
- 建议: `LayerLabel` 改为与 PassiveUI.ToRoman 一致，或直接复用 `UnlockChapterOf` 所在类内数组

### **Bug #2**（次要 / UI 不实时刷新）
- 位置: `Assets/Scripts/UI/PassiveUI.cs:44-53`
- 环境: 被动面板打开状态下执行 SetChapter / AdvanceChapter
- 现象: PassiveUI 仅订阅 PassiveSlotsChangedEvent，未订阅 ChapterChangedEvent（全代码库检索 `Subscribe<ChapterChangedEvent>` 为零）；章节变化时已打开的面板不刷新，锁状态/标题停留在旧章节
- 复现步骤: 进游戏 → 打开被动面板 → 控制台执行 `passiveEquipManager.SetChapter(3)` → 面板仍显示 T2/T3 锁定；关闭并重新打开面板后 T2/T3 才显示解锁
- 预期: 章节变化后面板立即反映新解锁状态；实际: 需重新开面板
- 影响: 低。章节推进发生在关卡切换场景（面板通常已关闭）；但对"控制台改章节验证"场景有误导性
- 建议: OnEnable 增加 `EventBus.Subscribe<ChapterChangedEvent>(e => Refresh())`，OnDisable 对称退订

---

## 四、其他发现（非缺陷）

| # | 发现 | 说明 |
|---|---|---|
| N1 | 场景残留旧字段 | SampleScene.scene L34263 仍序列化 `unlockLevels`（1/5/8/12/16 旧等级数据），当前类已无此字段，Unity 加载时自动忽略，无功能影响；建议下次保存场景时清理 |
| N2 | SkillData.unlockLevel 遗留 | 基类 SkillData.unlockLevel 仍存在，编辑器工具（CreatePassiveSkillDataAssets.cs）写入 SO，但运行时被动解锁已完全不读取，属遗留数据 |
| N3 | 战斗态进出配对 | 攻击路径 refCount 上限 2（PlayerCombat.TryAttack 守卫 + OnCombatAction 防重各 +1），由 PlayerController.combatTimer(3s) 与 PlayerCombat.combatTimeoutTimer(5s) 两条定时器配对递减归零，时序错峰但不失配；敌人侧 EnemyControllerBase.OnEnter/OnExitCombatState 有 isInCombatState 守卫 |

---

## 五、验证程序产物

- 验证工程: `G:\unity\verify_passive\`（Program.cs 11 组测试，含新增 [10] JsonUtility 旧档兼容、[11] 装备T2存档读档闭环）
- 运行结果: **50 PASS / 0 FAIL / 1 SKIP**（SKIP 为 JsonUtility 纯 .NET ECall 限制，已源码双路径兜底）
- 编译证据: Assembly-CSharp.dll 强制重编译 0 错误（10:27），Editor 程序集 0 错误

---
**结论：被动章节解锁改造与防重 Bug 修复实现与任务描述一致，8 项验证清单全部通过；2 个次要缺陷（日志文案、面板章节刷新）建议排期修复，不阻断本功能上线。**
