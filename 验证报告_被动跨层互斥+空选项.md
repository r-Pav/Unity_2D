# 验证报告：被动跨层互斥 + 空选项

- 任务: t_3c1e1d8b（programer 实现 t_24894b23）
- 验证人: profile_unity-tester
- 日期: 2026-08-03
- 环境: Tuanjie 2022.3.62t6 / 项目 `G:\unity\Tuanjie project\My project 2D\project`
- 改动文件: PassiveEquipManager.cs / PassiveUI.cs / LineSelectDialog.cs / SaveSystem.cs / Events.cs / SampleScene.scene / 25×Passive_L*_L*.asset(图标)

## 验证方式

| 手段 | 说明 | 结果 |
|---|---|---|
| 编译检查 | `dotnet build Assembly-CSharp.csproj` + `Assembly-CSharp-Editor.csproj`（Unity 生成工程，含全部 127 脚本，当前源码 15:34-15:40 修改） | 0 错误（12+1 警告均为既有 CS0649/CS0414 类） |
| 自动化逻辑验证 | `G:\unity\verify_passive_mutex\`（纯 .NET 反射 harness，引用编译产物 Assembly-CSharp.dll） | 61 PASS / 0 FAIL |
| 场景接线审查 | 逐字段核对 SampleScene.scene 的 optionButtons / lineDialogOptions / LineSelectDialog 子物体 | 发现 Bug #1 |

> 注：Unity 编辑器当前占用项目且未重编译最新源码（Temp DLL 停留在 10:27、Library DLL 14:49，均早于 15:34 改动）。本次编译检查用 dotnet 对 Unity 生成的 csproj 完成，与编辑器同文件集同引用，0 错误。编辑器下次刷新时会自行重编译。

## 逐条验证结果

| # | 验证项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 代码编译无错误 | ✅ 通过 | dotnet build Assembly-CSharp: 0 errors / 12 warnings；Assembly-CSharp-Editor: 0 errors / 1 warning |
| 2 | T1槽0=线0, T2槽0=线1 → T1灰色半透明, 属性仅T2线1生效 | ✅ 通过 | harness [2]：IsSlotSuppressed(0,0)=true、GetEffectiveInColumn(0)=(1,1)、GetCumulativeModifiers(1)=1条 / (0)=0条；灰显颜色 `(0.5,0.5,0.5,0.35)` 源码确认 PassiveUI.cs:210 |
| 3 | T1槽0=线0, T2槽0=空 → T1线0正常显示+属性生效 | ✅ 通过 | harness [3]：T2槽值=-2、IsSlotSuppressed(0,0)=false、生效列=(0,0)、累计 value=20 |
| 4 | T1线0 / T2空 / T3线2 → T3生效, T1被压制 | ✅ 通过 | harness [4]：IsSlotSuppressed(0,0)=true、(2,0)=false、生效列=(2,2)、line2累计1条 / line0累计0条 |
| 5 | 右键卸下T3 → 列回退: T2空跳过, T1线0重新生效 | ✅ 通过 | harness [5]：UnequipPassive(2,2)=true、T3槽=-1、IsSlotSuppressed(0,0)=false、生效列=(0,0)、line0累计恢复=20 |
| 6 | 选"空"不受同层重复限制 | ✅ 通过 | harness [6]：T3已装线0/线1 仍可 EquipPassive(2,-2,2)=true；同层重复线0仍被拒；空可覆盖旧技能 |
| 7 | GetCumulativeModifiers 仅统计非压制层 | ✅ 通过 | harness [7]：T1线0(20,被压制)+T3线0(40) → 累计仅1条 value=40（非 60） |
| 8 | 存档读档含-2配置正确恢复, 属性正确生效 | ✅ 通过 | harness [8]：15槽含-2全部恢复；列生效 (4,2)/(4,0)/(3,4)；被压制线累计为0 |
| 9 | 战斗中面板按钮置灰不可操作(不退化) | ✅ 通过 | harness [9]：InCombat=true 时 Equip/Unequip 被拒；SetUIPauseOverride(true) 后 InCombat=false、面板可操作（不退化）；退出战斗恢复 |
| 10 | 旧存档(无-2)读档正常, -1/0~4兼容 | ✅ 通过 | harness [10]：仅 -1/0~4 读档无异常、15槽全部恢复、列生效正常 |

补充验证: PassiveSlotsChangedEvent 新增 "empty" action 且 lineId=-2 透传（harness [11]）；SaveSystem.CollectPassiveSlots 走 GetEquippedLineId（int 原生支持 -2），RestorePassiveSlots 两遍式（先写 slots 再 RecalculateColumn，SaveSystem.cs:375-389 → PassiveEquipManager.cs:655-676），结构不变，新旧存档互通。

## Bug 列表

### **Bug #1** — [严重 P1] 场景未添加"空"按钮，第5个线按钮被劫持为空选项

- **环境**: 运行时（SampleScene.scene + LineSelectDialog.cs + PassiveUI.cs）
- **复现步骤**:
  1. 打开被动面板，点击任意槽位弹出 LineSelectDialog
  2. 观察 5 个选项按钮：线1~线5（无"空"按钮）
  3. 点击"线5"按钮
- **预期结果**: 装备线4（法力+CD）
- **实际结果**: 调用 `Select(EmptyChoice)` 装备"空"(-2)，槽位被清空；且线4（法力+CD）永远无法从 UI 装备
- **根因**: 代码约定"最后一个按钮=空"（LineSelectDialog.cs:38-40 `isLast = i == optionButtons.Length - 1`；PassiveUI.cs:122 `isEmptyOption = i == optionCount - 1`），但场景只挂了 5 个线按钮：
  - LineSelectDialog.optionButtons（scene:27216-27221）= 5 项 [16916953, 25080598, 1139905727, 417006954, 514157254]
  - PassiveUI.lineDialogOptions（scene:25831-25837）= 同样的 5 项
  - LineSelectDialog 子物体（scene:27127-27134）= title + 5 线按钮 + 残留 CloseBtn(2140829042)
  - 按钮文案：线1(31385)/线2(26124)/线3(50538)/线4(20898)/线5(20366)，全场景无 "空"(u7A7A) 文案
- **影响**: ① 线4 不可装备（功能缺失）；② "空"选项以误导性"线5"按钮形式存在，玩家点击会清空槽位；③ 旧存档中已装线4的玩家，点"线5"会误清空
- **修复建议**: 场景中在 LineSelectDialog 下新增第 6 个按钮（文案"空"），追加到 optionButtons 与 lineDialogOptions 两个数组末尾（保持索引 0~4=线1~5、索引5=空）

### **Bug #2** — [中 P2] 解除压制后槽位线名文字残留半透明（不随图标恢复）

- **环境**: 运行时（PassiveUI.cs SetSlotState）
- **复现步骤**:
  1. T1槽0=线0、T2槽0=线1 → T1 线名文字显示为半透明白(1,1,1,0.35)（被压制态）
  2. 右键卸下 T2 线1 → T1 解除压制
  3. 观察 T1 线名文字
- **预期结果**: 文字恢复不透明（图标已恢复白）
- **实际结果**: 文字 alpha 仍为 0.35（半透明），图标却已恢复正常白 — 视觉不一致
- **根因**: PassiveUI.cs:225-228 仅 `if (suppressed) lineName.color = ...`，无 else 分支复位；对比 icon.color（:209-212）有 if/else 完整复位
- **影响**: 仅视觉残留，功能不受影响
- **修复建议**: 加 else 分支复位 `lineName.color = Color.white`（或由 interactable 决定）

### **Bug #3** — [低 P3] LineSelectDialog 残留不可用的 CloseBtn

- **环境**: SampleScene.scene LineSelectDialog 子物体 2140829042（m_Name: CloseBtn，scene:54379），未被任何数组引用，onClick 无回调
- **实际结果**: 对话框打开时右上角显示一个无功能的关闭按钮（遗留自 0e3ff04 "删CloseBtn" 提交，仅删了引用未删物体）
- **建议**: 删除该 GameObject 或补接线

## 自动化验证产物

- 编译产物（当前源码）: `project\Temp\bin\Debug\Assembly-CSharp.dll`（15:55 由 dotnet 编译）
- harness: `G:\unity\verify_passive_mutex\Program.cs` + `passive_mutex_verify.csproj`
- 运行结果: `G:\unity\verify_passive_mutex\run_result.txt` — 61 PASS / 0 FAIL

## 结论

代码层实现（PassiveEquipManager/PassiveUI/LineSelectDialog/SaveSystem/Events）与方案一致，10 项验证全部通过（逻辑 61 断言全绿 + 编译 0 错误）。
但 **场景接线未完成**（Bug #1 P1）：缺"空"按钮、线4 被劫持，功能在实机 UI 上不可正确使用。需开发同学补场景后回归验证 #2、#6 的 UI 呈现。

---
验证完毕（测试完毕）
