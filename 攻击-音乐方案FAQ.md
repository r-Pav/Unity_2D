# 「攻击-音乐」战斗方案 FAQ(v2 最终版)

> 本文档为「攻击-音乐」方案的最终结论。v2 相对 v1 的变更:
> 1. 删除「BPM→攻击速度」映射,攻击速度回归动作设计,与 BGM 无关
> 2. 删除「拍号→连段数」映射,连段系统不被音乐绑架
> 3. 特殊攻击=背刺变体:窗口内触发,动画同时闪现至敌人后方
> 4. 窗口外按特殊键=普通攻击挥空(原「完全无反应」废弃)
> 5. 触发判定从音乐点反推 1-2 帧,减少输入到响应的延迟感

**方案一句话概括**:以 BGM 驱动特殊攻击。每首曲子手工标记少量"音乐点"(可能只有几个,并非每个重音都要做),点附近窗口内按特殊键→强制打断当前动作→播放特殊攻击动画并闪现至敌人后方,出伤/音效由动画事件控制;窗口外按→普通攻击挥空。定位是"节奏增伤",而非"强制节奏"。

| 音乐参数 | 对应战斗参数 | 说明 |
| --- | --- | --- |
| 音乐点(手工标记) | 特殊攻击触发时机 | 每曲只标少量点,不追求密度 |
| ~~BPM → 攻击速度~~ | 已删除 | 攻击速度由动作设计决定 |
| ~~拍号 → 连段数~~ | 已删除 | 连段系统独立于音乐 |

## 一、方案基础与定位

**Q1:方案的核心思路是什么?**

A:特定音乐点→特殊攻击。在音乐中手工标记的位置,是特殊攻击唯一会出现的时机。不做 BPM/拍号映射,一首曲子可能只有几个点。

**Q2:特殊攻击的定位是什么?**

A:只在音乐点窗口出现,是背刺类攻击变体。玩家在窗口内按键触发,强制打断当前动作执行;错过就错过,不存在"必须踩中"的强制要求。

**Q3:玩家没踩上音乐点,伤害怎么算?**

A:正常伤害。普通攻击照常执行、照常结算,音乐点机制是节奏增伤性质的补充,不构成玩法门槛。

## 二、时间轴与同步

**Q4:音乐点位置如何确定?需要实时音频检测吗?**

A:不做实时检测。写一个脚本,在脚本里手工设置对应音乐的时间轴,标记"这首音乐在哪些时间点有音乐点"。每曲只标少量点。

**Q5:整套同步的时间参照是什么?**

A:统一以音频的时间轴(AudioSource.time)为参照,不用独立的系统计时累加。所有与音乐点相关的判断都从音频播放位置反查。

**Q6:音乐循环播放会不会导致音乐点错位?**

A:不会。以音频时间轴为统一参照后,不存在该问题。

**Q7:顿帧(受击停顿等)时怎么处理?**

A:统一一个 bool 参数,通过它确定音频是否需要处理。即使音频被处理(变慢、卡顿),也不影响音乐点标记——判断仍然基于音频时间轴,天然保持一致。
(注:本项目卡帧只冻视觉,音频正常走,因此顿帧期间音乐点窗口照常流失,玩家被冻住时无法按键,属预期行为,无需额外处理。)

**Q8:BPM 高低需要处理吗?**

A:不需要。攻击速度与 BGM 无关,由动作设计决定;音乐点只标少量,不看重音密度。

## 三、输入、触发与状态

**Q9:特殊攻击如何触发?**

A:由独立的按键触发(与普通攻击按键分开)。窗口内按下→强制打断当前动作→执行特殊攻击;不按,则照常执行当前动作。

**Q10:玩家在音乐点窗口之外按了特殊键,会怎样?**

A:执行普通攻击挥空(无伤害、有挥空反馈)。不吞输入,让玩家明确知道"没踩上",而不是以为按键坏了。

**Q11:触发窗口的宽度怎么配置?**

A:全局统一调整,不针对每首曲子单独设置。窗口宽度和全局延迟 offset 放在一起调。另外,触发判定从音乐点反推 1-2 帧(提前量),让动画提前启动、伤害落点更贴,减少延迟感。

**Q12:触发特殊攻击后,状态如何管理?**

A:特殊状态统一处理,锁掉一部分按键。具体规则:

- 普通连段有自己的生命周期,时间过了就过了,特殊攻击结束后连段重置,不延续;
- 在特殊攻击动画的起始和结束各做一个事件,用于锁定特殊攻击按键,防止动画期间重复触发。

## 四、手感对齐

**Q13:"音乐点触发特殊攻击"的具体语义是什么?**

A:窗口内按下→播放特殊攻击动画,同时将玩家移动至敌人后方(闪现);伤害结算、音效由动画的事件帧控制;位移与动画第一帧同时进行。

**Q14:需要延迟补偿吗?**

A:需要。存在一条延迟链:音频输出延迟 → 玩家按键 → 触发特殊攻击动画 → 动画事件帧出伤害,需要一个全局 offset 参数来补偿。触发判定额外反推 1-2 帧,让动画启动时刻提前,伤害命中时刻更贴近音乐点。

调整手感时要注意:offset 对齐的目标是"伤害命中的时刻"对上音乐点,而不是"动画启动的时刻"对上音乐点——两者相差一个动画前摇时间,调的时候不要混。

## 五、提示设计

**Q15:玩家如何知道音乐点要来了?**

A:做视觉提示:用一个特效或图片(例如一个慢慢收紧的圆圈),其出现时机根据曲子配置的音乐点位置反推得出。预告是必需品,不做预告玩家只能纯听歌猜点。

## 六、遗留事项与建议

1. **挥空反馈**:窗口外挥空配一个提示音/特效,让玩家明确"没踩上"。反馈音效时长按实际音乐调整,若音乐点密集注意音效叠盖。
2. **开场缓冲**:建议第一首曲子的第一个音乐点前留一段前奏缓冲,避免开局即点。
3. **调试辅助**:建议在调试时显示"当前音频时间 / 距下一个音乐点时间",可以显著加快时间轴的标定和验证。
4. **验证路径**:建议先用一首曲子、三五个音乐点,把"预告 → 窗口 → 打断 → 闪现 → 伤害对齐"这条完整链路跑通、把手感调舒服,再铺开做其余曲子的内容。
